# Mongo

MongoDB document store for NoSQL demos.

This is a stub. The full reference is written in Phase 8 (documentation site).

- Image: `mongo:7` (override with `MONGO_IMAGE` in `.env`).
- Kind: database.
- Network when split: backend.
- Provides: document-store.
