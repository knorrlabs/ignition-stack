## Reverse proxy

{{ reason }}

{% if proxy_mode == "scaffold" -%}
- **Read the scaffolded README:** `{{ proxy_path }}/README.md`
- Clone etknorr/traefik-reverse-proxy into `{{ proxy_path }}/` and bring it up
  (`docker compose -p proxy up -d`) so it creates the `{{ proxy_network }}` network
  this stack joins.
{% else -%}
- Make sure your proxy is up and owns the external `{{ proxy_network }}` Docker
  network (the default etknorr/traefik-reverse-proxy creates).
{% endif -%}
- The gateways are routed at:
{% for route in proxy_routes %}
  - **{{ route.role }}:** {{ route.url }}
{%- endfor %}
