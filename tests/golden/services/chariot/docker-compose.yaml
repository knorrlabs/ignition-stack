---
# Generated by ignition-stack. 1 Ignition 8.3 gateway, chariot.
x-ignition-common: &ignition-common
  image: &ignition-image ${IGNITION_IMAGE}

x-ignition-environment: &ignition-environment
  ACCEPT_IGNITION_EULA: "Y"
  GATEWAY_ADMIN_USERNAME: ${ADMIN_USERNAME}
  GATEWAY_ADMIN_PASSWORD: ${ADMIN_PASSWORD}
  IGNITION_EDITION: standard
  TZ: ${TZ}

services:
  bootstrap:
    image: *ignition-image
    user: root
    entrypoint: ["/bin/bash", "/docker-bootstrap.sh"]
    environment:
      GATEWAY_NAME: ${GATEWAY_NAME}
    volumes:
      - ignition-data:/data
      - ./scripts/docker-bootstrap.sh:/docker-bootstrap.sh:ro
      - ./services/ignition:/template-source:ro

  gateway:
    <<: [*ignition-common]
    container_name: ${GATEWAY_NAME}
    hostname: ${GATEWAY_NAME}
    depends_on:
      bootstrap:
        condition: service_completed_successfully
    ports:
      - "${GATEWAY_HTTP_PORT}:8088"
    volumes:
      - ignition-data:/usr/local/bin/ignition/data
    environment:
      <<: *ignition-environment
    command: >
      -n ${GATEWAY_NAME}
      -m 2048
      --
      -Dignition.config.mode=dev

  chariot:
    image: ${CHARIOT_IMAGE}
    hostname: chariot
    container_name: chariot-${COMPOSE_PROJECT_NAME}
    environment:
      TZ: ${TZ}
      ACCEPT_EULA: "true"
      ADMIN_PASSWORD: ${CHARIOT_ADMIN_PASSWORD}
    ports:
      - "${CHARIOT_HTTP_PORT}:8080"
      - "${CHARIOT_MQTT_PORT}:1883"

  chariot-trial:
    image: ${CHARIOT_IMAGE}
    container_name: chariot-${COMPOSE_PROJECT_NAME}-trial
    restart: "no"
    depends_on:
      - chariot
    entrypoint: ["/bin/sh", "/chariot-trial.sh"]
    environment:
      # Chariot's web admin is the fixed image user `admin`; ADMIN_PASSWORD on the
      # broker service sets its password. The script defaults the user to `admin`.
      CHARIOT_HOST: chariot
      CHARIOT_ADMIN_USER: admin
      CHARIOT_ADMIN_PASSWORD: ${CHARIOT_ADMIN_PASSWORD}
    volumes:
      - ./services/chariot/chariot-trial.sh:/chariot-trial.sh:ro

volumes:
  ignition-data:
